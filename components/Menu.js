import React from "react";
import Button from "@/components/Button";
const Menu = () => {
  return (
    <div className="absolute bottom-40 right-4">
      <Button href="/add" color="" text="Add"></Button>
    </div>
  );
};

export default Menu;
